first_line=useless
# Due to a bug somewhere in reading files, first character gets a different value
# For that reason first line is useless
# Icon defaults to appicon.bmp and then desktop's appicon if it doesn't exist
icon=appicon.bmp
name=Simple win
executable=/apps/simplew/simplew


